import { createRouter, createWebHistory } from 'vue-router'

// Your pages will go here
const routes = [
  // Example: { path: '/', component: HomePage }
]

const router = createRouter({
  history: createWebHistory(),
  routes,
})

export default router